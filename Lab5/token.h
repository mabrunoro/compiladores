
#ifndef TOKEN_H
#define TOKEN_H

typedef enum {
    NUM,
    PLUS,
    MINUS,
    TIMES,
    OP,
    CP,
    ENTER,
	SEMI,
	IF,
	THEN,
	ELSE,
	END,
	REPEAT,
	UNTIL,
	READ,
	WRITE,
	OVER,
	EQ,
	LT,
	ASSIGN,
	ID
} TokenType;


#endif // TOKEN_H
